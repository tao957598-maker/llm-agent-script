/*
{
  "title": "Functional List 转 Markdown",
  "description": "将 functional_list 数据通过 GPT 转换为 Markdown 格式",
  "parameter": [
    {
      "name": "functional_list",
      "label": "Background列表数据"
    }
  ],
  "llm_type": "deepseek-chat",
  "life": "100",
  "modified_by": "lht",
  "resultPlan": "n_functional_md_result",
  "modified_time": "2024-11-28",
  "version": "1"
}
*/

// 定义 GPT 模板



define('tpl_functional_to_md', '''
# 要求
**任务目标**：
1.请将内容信息提取成总结性的Markdown内容,
2.Markdown内容没有一级标题
3.Markdown只有一个2级标题,标题定义: 根据`内容信息`和requirement_type提取
4.Markdown内容从3级标题开始
5.使用Markdown文本格式返回:
```markdown
提取的Markdown文本内容
```
### ✅ 拆解原则
1. **使用连字符“-”连接上下文与具体页面**，体现模块所在路径或导航关系，保留模块层级语义。
2. 标记来源哪些页码例如: Pagexx,Pagexxx
3. 语言风格: 遵从文档原始术语（如标题中是英文的）保持一致更好

# 内容信息
{{functional_list}}
''')

// 调用 GPT 进行转换
n_functional_md_result, tpl_functional_to_md,psr_md

// 保存转换结果
assign('functional_md_result', '{{n_functional_md_result}}')

// 输出结果
assign('result', '{{functional_md_result}}')

// 捕捉失败定义运行失败
on_failure({
  assign('result', 'fail')
  act_flow.go?plan=end
})