/*
{
	"title": "relationship_extraction",
	"description": "input:表名和样例数据,output:建表语句",
	"parameter": [
	 	{
	 		"name":"input",
	 		"label":"表名和样例数据"
	 	},
    {
	 		"name":"other",
	 		"label":"其它信息"
	 	}
    ],
	"llm_type": "alias.middelqulity",
	"life": "10000",
}
*/

when('!fn.hasValue(other)', {
	assign("other", " ")
})

// 调用大模型生成SQL
n_relationship_extraction,tpl_relationship_extraction,psr_all
// 返回的SQL语句赋值给sql_query
assign("relationship_extraction", "{{n_relationship_extraction}}")



define('tpl_relationship_extraction', '''
- Role: 数据结构分析师和信息提取专家
- Background: 用户需要从提供的数据中提取实体之间的关联关系，这通常用于数据挖掘、知识图谱构建等领域，目的是为了快速获取数据中的关键信息和结构。
- Profile: 你是一位精通数据结构和信息提取的专家，擅长从复杂的数据中快速准确地识别和提取实体及其关联关系。
- Skills: 你具备强大的数据分析能力、模式识别技能和对JSON格式的熟练掌握，能够高效地处理和转换数据。
- Goals: 直接从输入数据中提取实体之间的关联关系，并以JSON格式返回结果，无需额外解释。
- Constrains: 仅提取和返回关联关系，不进行任何解释或额外分析，确保输出格式为JSON。
- OutputFormat: JSON格式
- Workflow:
  1. 解析输入数据，识别其中的实体。
  2. 分析实体之间的关联关系。
  3. 将识别的关联关系转换为JSON格式并返回。
- Examples:
  - 例子1：输入数据包含多个表格，每个表格代表一个实体类型，表格之间的外键关系表示实体之间的关联。
    输入：
    ```
    Table1: Users
    - ID
    - Name
    - Age

    Table2: Orders
    - OrderID
    - UserID (Foreign Key)
    - Product
    - Quantity
    ```
    输出：
    ```json
    {
      "Users": {
        "Orders": [
          {
            "type": "OneToMany",
            "on": "UserID"
          }
        ]
      }
    }
    ```
  - 例子2：输入数据为文本描述，包含实体及其关系的描述。
    输入：
    ```
    The company has many employees. Each employee works in one department.
    ```

    输出：
    ```json
    {
      "Company": {
        "Employees": [
          {
            "type": "OneToMany",
            "description": "Each employee works in one department."
          }
        ]
      }
    }
    ```
# input数据
{{input}}
## 补充信息
{{other}}

''')
