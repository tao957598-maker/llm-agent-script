在Rerank之前，是没有分数的

1、Milvus建表
2、插入数据










一、用户输入
	1、open url xxx   a -> b
	2、输入账号 输入密码 点击登录  b -> c
	5、点击Accounts 点击Primary   c -> d



	1、open url xxx		a -> d

	2、输入账号
	3、输入密码
	4、点击登录

	5、点击Accounts
	6、点击Primary





二、模块化判断
	1、输入内容生成测试步骤的时候, 由大模型首次判断是否为同一个模块

	2、假设首次判断模块失败, 在真正执行的时候, 判断当前步骤是否发生跳转
		如：
			执行前一个模块定义一个列表 []
			1、open url, 此时发生跳转, 此时模块为 [open url], 入库

			2、输入账号, 未发生跳转, 此时为：[输入账号], 不入库
			3、输入密码, 未发生跳转, 此时为：[输入账号、输入密码], 不入库
			4、点击登录, 发生跳转, 此时为：[输入账号、输入密码、点击登录], 入库



三、代码复用板块
	如何匹配？匹配内容还是匹配步骤？


	1、通过向量库匹配得到目标的页
		如：
			1、点击 aaa 下拉框 
			2、点击 bbb 选项
		一步步去向量匹配？还是两个连起来一起匹配
	2、通过图谱获取最短路径


一、用户输入
	1、open url
	2、输入账号
	3、输入密码
	4、点击登录

——》登录成功 + code

2、用户输入
	1、登录成功 
	5。xx
	6.xxx


3、用户输入
	1、open url
	2、输入账号
	3、输入密码
	4、点击登录
	5。xx11
	6.xxx11