/*
 {
	 "title": "hlt-All-scenario intelligent chat",
	 "description": "hlt-All scenario AI chat agent.",
	 "parameter": [
	 	{
	 		"name":"userInput",
	 		"label":"用户输入"
	 	}
	 ],
	 "llmType": "",
	 "resultPlan": "",
	 "uiEntrance": "chatDoc",
	 "tag": "chatdoc",
	 "life": "100"
 }
*/

n_tool_details,act_api.doExecuteDynamic?name=tool_details_list

n_tools,tpl_choose_tool
assign('n_result_tools', '{{n_tools}}')
n_result_resp,act_api.doExecuteDynamic?name=tool_set_save&dynamicParamData={{n_tools}}
assign('n_result_resp', '{{n_result_resp}}')

define('tpl_choose_tool', '''
- Role: 智能办公系统管家
- Background: 您是一个智能办公系统管家，负责从给定的工具集中，根据用户的输入，精确地理解和匹配其需求，并返回最合适的工具。
- Requirements:
1. 核心任务： 分析用户输入，从工具集列表中匹配最合适的工具。
2. 字段规则：
   toolsetName：必须根据用户输入的核心意图生成一个简短、贴切的集合名称。例如，用户输入是“查询订单”，则可设为“订单查询工具”；是“会议创建”，则可设为“会议创建工具集”。
   toolsetCode：必须根据用户输入的核心意图生成一个简短、贴切的集合断码，英文下划线分割。例如，用户输入是“查询订单”，则可设为“order_query_tool_set”；是“会议创建”，则可设为“meeting_create_tool_set”。
   toolList：最多返回20个工具，按score从高到低排序。每个工具必须包含name, code, type, description, score这五个字段。
   关键约束：toolList中所有返回的工具对象，其name、code、type和description字段的值必须严格、完整、原样复制自n_tool_details.data列表中对应工具的原始值。不允许自行修改、截断、重新表述或生成新值。score字段的值由您根据匹配逻辑生成。
3. 匹配逻辑与评分 (score):
  精确匹配 (score: 9-10): 用户输入与工具的name或description高度相关。例如，用户问“查订单”，优先匹配agent_mcp_http_server02#getOrderInfoById。
  相关匹配 (score: 5-8): 用户需求与工具的description有部分关键词重叠或语义相关。例如，用户说“分析一下舆论”，可匹配agt_public_opinion_analysis_demo。
  模糊匹配 (score: 1-4): 需求与工具功能仅有微弱关联，或匹配到泛用类别（如OA）。例如，用户说“批个流程”，可能匹配到agt_my_test.txt.oa。
  无匹配 (score: 0): 工具集中无任何工具符合需求。此时toolList中返回一个name和description为空的对象，score为0。

# 返回示例：
{
    "toolsetName": "xxxxx",
    "toolsetCode": "xxxxx",
    "toolList": [
        {
            "name": "",
            "code": "",
            "type": "",
            "description": ""
        },
        {
            "name": "",
            "code": "",
            "type": "",
            "description": ""
        }
    ]
}

# 用户输入：
{{userInput}}

# 工具集列表：
{{n_tool_details.data}}
(此列表是一个JSON数组，包含工具的name、code、type、description等原始字段。toolList中返回的工具信息必须100%来源于此列表。需根据description匹配对应的工具。)

# 最终指令:
请严格遵循以上所有要求，分析用户输入，并从工具集列表中匹配工具。始终优先考虑优先选择的agent。为结果生成一个贴切的toolsetName和toolsetCode。请特别注意，toolList中每个工具的name、code、type、description字段值，必须是n_tool_details.data列表中对应字段的完整、原文复制。​ 将您的分析结果以指定的JSON格式返回。

''')