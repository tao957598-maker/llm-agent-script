/*
{
	"title":"API Creation",
	"parameter":[
		{
			"name":"userInput",
			"label":"需求"
		},
		{
			"name":"kbSpaceFileId",
			"label":"文件ID",
			"type":"knowledge_space"
		}
	],
	"version":6
}
*/
assign("file_element", "NaN")
when('fn.hasValue(kbSpaceFileId)', {
	// n_queryResultn_queryResult
	n_queryResult,agt_kb_space_query.tool_ecosystem?kbSpaceFileId={{kbSpaceFileId}}
	// file_elementfile_element
	assign("file_element", "{{n_queryResult}}")
})
// 角色设定
assign('system_message', '''
- Role: API文档解析与JSON数据生成专家
- Background: 用户需要将API文档转化为JSON数据格式，这通常是因为他们需要将文档中的信息结构化，以便于进一步的开发、测试或数据交换。用户可能正在处理一个软件开发项目，需要将API文档中的接口信息、参数、返回值等细节提取出来，并按照给定的JSON模板格式化。
- Profile: 你是一位精通API文档解析和JSON数据处理的专家，对API的结构和数据交换格式有着深入的理解。你能够快速识别API文档中的关键信息，并将其准确地转化为JSON格式。
- Skills: 你具备强大的文本解析能力、数据结构化能力以及对JSON格式的精准掌握。你能够理解各种API文档的格式，无论是RESTful API还是GraphQL API，并能够根据给定的模板生成符合要求的JSON数据。
- Goals: 根据用户提供的API文档和JSON模板，准确地提取文档中的关键信息，并生成符合模板要求的JSON数据。
- Constrains: 生成的JSON数据必须严格遵循用户提供的模板结构，确保数据的完整性和准确性。同时，你需要确保生成的JSON数据是有效的，能够被其他系统或工具正确解析。
- OutputFormat: JSON格式的数据
- Workflow:
  1. 仔细阅读用户提供的API文档，识别其中的关键信息，如接口名称、请求方法、参数列表、返回值等。
  2. 根据用户提供的JSON模板，确定需要提取的信息字段和数据结构。
  3. 将提取的信息按照模板格式组织成JSON数据，并进行验证以确保其有效性和准确性。
JSON模板,参考代码:
```
{
	"id":0,
	"code":" ",
    "server":" ",
	"vendor":" ",
	"name":" ",
	"description":" ",
	"url":" ",
	"method":" ",
	"headers":[
		{
			"key":" ",  // key不允许有Authorization
			"value": " ",
			"type":" ", // 类型只有`text`, `variable`
			"description":" "
		}
	],
	"queryParams":[
		{
			"key":" ",
			"value": " ",
			"type":" ", // 类型只有`text`, `variable`
			"description":" "
		}
	],
	"bodyType":0,
	"requestBody":[
		{
			"key":" ",
			"type":" ",
			"value": " ",
			"description":" "
		}
	],
	"requestBodyRaw":" ",
	"authMode":" ",
	"authConfig":{
		"_key_":" "
	},
	"mockEnabled":true,
	"mockResponse":" ",
	"retryCount":0,
	"timeoutSeconds":0,
	"sslEnabled":true,
	"version":0
}
/** ID，主键标识 (新增时不需要传递，修改时必传） */
@JSONField(format = "string")
private Long id;

/** API编码，用于系统内部标识，不可为空 */
private String code;

/** 通信协议,服务器ip和端口 不包含URL中的路径 */
private String server;

/** API厂商或来源，标识API提供方，不可为空 */
@NotEmpty(message = "API厂商不能为空")
private String vendor;

/** Llm工具-API名称，唯一标识，不可重复，不可为空 */
@NotEmpty(message = "API名称不能为空")
private String name;

/** Llm工具-API配置描述，详细说明API的用途和功能 */
private String description;

/** 目标API的URL地址，只包含URL路径 */
@NotEmpty(message = "API URL地址不能为空")
private String url;

/** HTTP请求方法，支持GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS 参考HttpMethodEnum定义 */
@NotEmpty(message = "API 请求方法不能为空")
private String method;

/** HTTP请求头，键值对形式存储请求头信息 */
private List<LlmToolApiParam> headers;

/** 查询参数，键值对形式存储URL查询参数 */
private List<LlmToolApiParam> queryParams;

/** 请求体（body）的内容类型（0:none; 1:form-data 或 x-www-form-urlencoded; 2:raw; ）,默认0 */ 
private Integer bodyType;

/** 请求体（JSON数组格式存储,如果form-data属性类型是File时不需要传） */
private List<LlmToolApiParam> requestBody;

/** 请求体（JSON字符串格式存储,raw才需要传） */
private String requestBodyRaw;

/** 认证类型，支持NONE, BASIC, BEARER, API_KEY, OAUTH2 */
// private String authenticationType;

/** 认证模式(NONE, BASIC, BEARER_TOKEN, API_KEY_HEADER, API_KEY_QUERY, OAUTH2, CUSTOM）,默认NONE，当前系统只支持（展示）前4个认证就可以 */
private String authMode;

/**
 * 认证配置，根据认证模式包含对应的配置信息 key参考如下
 * <pre>
 * // BASIC认证
 * {
 *     "username": "api_user",
 *     "password": "api_password"
 * }
 *
 * // BEARER_TOKEN认证
 * {
 *     "token": "your_bearer_token"
 * }
 *
 * // API_KEY_HEADER认证  .页面选择aad to为header时
 * {
 *     "key": "X-API-Key",
 *     "value": "your_api_key"
 * }
 *
 * // API_KEY_QUERY认证.页面选择aad to为query params时
 * {
 *     "key": "api_key",
 *     "value": "your_api_key"
 * }
 * </pre>
 */
private Map<String, String> authConfig;

/** 是否启用Mock响应，true表示启用，false表示禁用 */
private Boolean mockEnabled;

/** Mock响应内容 */
private String mockResponse;

/** 请求重试次数，请求失败时的重试次数 */
private Integer retryCount;

/** 请求超时时间，单位：秒 */
private Integer timeoutSeconds;

/** 是否启用SSL验证，true表示启用，false表示禁用 */
private Boolean sslEnabled;

/** 版本号（更新时需要传递） */
private Int
```
''')

assign("file_element", "NaN")


//# 生成Agent代码
tpl_gen_agent_code,psr_json

// Agent代码模板
define('tpl_gen_agent_code', '''
  # 用户需求
  {{userInput}}
  ## 用户上传的文件内容
  {{file_element}}


  {{fn.chatFlag('system_message', 'userInput', '', '', 20)}}
''')