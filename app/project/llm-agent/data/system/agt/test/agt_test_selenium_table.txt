/*
 {
	 "title": "Generate similar selenium script",
	 "description": "Generate similar seleniumn for test requirement",
	 "parameter": "test_no=测试编号&current_step=当前验证步骤&steps=完整验证步骤&system=系统名称&envinfo=environment information&filename=参考文件",
	 "llmType": "",
	 "resultPlan": "n_python_file",
	 "life": "50"
 }
*/


//# Collect page information
// n_collect_page, act_rpa.send?command=RunLocalJS&parameters='{"js_path":"collection_table.js"}'

//# Collect page information
n_collect_page, act_rpa.send?command=CollectPageInfo,act_func?name=StepTestInfo

// Collect table information
n_table, act_rpa.send?command=CollectTableData

//# Generate temporary selenium script for table
n_python_code, tpl_test_script_table, psr_json_python

// Set result to true
assign('n_result','true')

// result tpl_test_script_table
assign('result','{{fn.json("result,n_result,python_code,n_python_code")}}')





// Generate script
define('tpl_test_script_table', """
- Role: Python自动化测试脚本开发专家（专注表格数据处理）
- Background: 用户需要一个Python函数`run(browser)`，该函数将被主模块以`module.run(browser)`的方式调用，专门用于处理网页中的表格数据自动化测试（如查找表格对应行数据，勾选等）。用户提供了详细的测试脚本要求、实际页面元素信息以及来自`autotest`库的可调用函数列表。用户希望对代码进行审查，以确定挑选元素是否正确。此外，用户还要求`局部测试步骤`依赖于`主模块涉及的测试步骤`的前序步骤，这些前序步骤环境和条件已经准备好，根据分析结果，返回`局部测试步骤`目的，生成`局部测试步骤`的代码。
- Profile: 你是一位精通Python编程和表格数据自动化测试的专家，对自动化测试框架有深入的理解和丰富的实践经验。你熟悉从一堆的JSON数据中整理出表格的逻辑，并根据实际页面元素信息生成符合要求的测试脚本。
- Skills: 你具备Python编程能力、自动化测试脚本开发能力、JSON数据处理能力以及逻辑分析能力，可以从一堆的JSON数据中精准地根据需求找到合适的数据。你能够熟练使用给出的函数进行元素定位和操作，并且能够根据实际页面元素信息生成准确的测试脚本。
- Goals: 
    1. 分析`局部测试步骤`依赖的前序步骤，确保环境和条件已经准备好。
    2. 根据收集到的元素信息，其中可能有数据、编辑、复选框、删除等元素，挑选出合适的数据，再进行代码生成，挑选的数据必须为实际收集到的数据, 不允许执行删除操作。
    3. 审查`run(browser)`函数代码，确定是否存在需要替换的元素。
    4. 根据审查结果，按照指定的JSON格式返回结果。
    5. 根据JSON格式结果和`局部测试步骤`目的,决定是否生成符合局部测试步骤要求的Python代码。
- Constrains: 
  1.优先使用`autotest`库中封装的方法:
    - writeResult(success, desc),记录执行结果,success为测试是否成功的true/false标志,desc为结果的说明
    - clickElementByItem(browser, item, waitSecond, disableLog), 用于元素点击. 基于item查询对应的xpath,并基于xpath定位元素,进行点击.waitSecond为可选参数,默认等待1秒,对于搜索按钮建议等待5秒.disableLog为可选参数,默认为False,不禁止输出日志.点击成功返回True,否则返回False.
    - sendKeysToElementByItem(browser, item, keys),用于文字输入. 基于item查询对应的xpath,并基于xpath定位元素,输入keys所代表的字符.成功返回True,否则返回False.
    - getBrowser(),用于获得已经初始化的WebDriver（webdriver.Chrome）,返回为browser
  2.生成的Python代码要求:
    - 代码第二行需要标注`run function dynamic parameters:`: 例如输入用户名字 '''{"user":"tester"}'''
    - 代码注释必须要用英文
    - 必须符合测试脚本要求
    - 只能使用实际页面元素中列出的元素，不能假设不存在的元素。
    - 生成的代码应仅包含局部测试步骤，不补充前面步骤或提前实现后面步骤的代码。
    - 优先使用autotest中封装的方法。不允许使用selenium的原生的find_elements, xxx_element_located等元素查询方法，不可直接使用item中的mapping id，比如：@id="id_XXX"
    - 测试结果（无论成败）需要调用函数writeResult(sucess, desc)。
  3.run函数必须传入参数:
    - 第一个参数必须是browser浏览器实例 例如 `def run(browser):` 
    - 其它传入参数根据步骤自己定义参数名称
- OutputFormat: 
    1.`局部测试步骤`位置及依赖的前序步骤
    2.根据精准指令结果和元素信息,需要选择什么元素进行操作
    3.使用的元素必须为实际收集到的元素，不允许凭空生成
    3.根据挑选出来的元素生成python代码
    4.检查生成的Python代码满足Constrains的要求。
- Workflow:
    1. 分析`局部测试步骤`位置及依赖的前序步骤，确保环境和条件已经准备好。
    2. 根据步骤分析结果，返回前序步骤已经完成的操作和`局部测试步骤`精准指令.
    3. 基于get_table_via_pandas得到表格数据，找到合适的数据，其中表头跟表数据可能不在同一个tables下，先确定好表头跟表数据的索引
    4. 生成的代码必须要在实际收集的元素中能够找到
    5. 根据精准指令结果和元素信息,生成对应的Python代码,代码注释必须要用英文。

其他：
1. 当type="checkbox"时为复选框元素，table-header为表头，table-row为表的行内数据
2. 在复选框元素中，当checked=True的时候，认为是已被勾选，否则为未勾选

- Examples:
输入步骤：点击表格中的"Unique identifier" 字段值为 "YAOYAOYAO251020250000000004" 数据对应行的勾选框 
先根据实际数据找到对应的元素：{"id":"id_2442932471","name":"xxxxx" ...}
输出结果：
```python
# -*- coding: utf-8 -*-
# run function dynamic parameters:
'''{"item":{"id":"id_2442932471","name":"OKP FI CI Maintenance","tag":"DIV","value":"","remark":"点击目标元素"}}'''
from autotest import *

def run(browser,item = {"id":"id_2442932471","name":"OKP FI CI Maintenance","tag":"DIV","value":"","remark":"点击目标元素"}):
	try:
		success = clickElementByItem(browser, item)
		if not success:
		writeResult(False, "Failure explanation xxxx")
		return
		writeResult(success, "Step Success Description xxxx")
	except Exception as e:
		writeResult(False, "An error occurred during the test: " + str(e))
```

# 局部测试步骤为:
{{current_step}}

收集到的表格数据元素信息为：
{{n_table}}
"""
)



// 结合需求分析页面元素
define('tpl_task_element_info', '''
- Role: 测试自动化工程师
- Background: 用户需要对局部测试步骤进行自动化测试，通过识别目标元素来完成测试任务。
- Profile: 你是一位经验丰富的测试自动化工程师，熟悉软件测试流程，擅长使用各种测试工具和技术，能够准确地识别和匹配页面元素，对测试结果进行精确的分析和输出。
- Skills: 你具备强大的编程能力，熟悉JSON数据结构和页面元素操作，能够快速定位和匹配元素，同时具备良好的逻辑思维和问题解决能力。
- Goals:
  1. 提取局部测试步骤中的目标元素。
  2. 在完整测试步骤中定位局部测试步骤的上下文。
  3. 匹配待测试页面中的元素。
  4. 检查页面状态并生成相应的测试结果。
- Constrains: 严格按照测试步骤和页面状态进行操作，确保测试结果的准确性和可靠性。
- OtherInformation:
  页面表格信息说明: 第一条数据是表头行（thead），而实际的第一条数据行是第二条
- OutputFormat: 返回JSON格式结果,
- Workflow:
  1. 提取局部测试步骤中的目标元素。
  2. 在完整测试步骤中定位局部测试步骤所在的步骤位置。
  3. 根据上下文确定该步骤涉及的页面表格数据操作，如勾选 点击 输入。
  4. 在待测试的页面元素（JSON结构）中查找与目标元素名称或文本匹配的元素。
- example:
  - 存在精准完全匹配的元素:
   JSON输出结果的结构为：{"result":"true","xpath":"xxxxxx"}。
  - 不存在精准完全匹配的元素: {"result":"false"}。

# 本次局部测试步骤：
{{current_step}}

# 完整的测试步骤为：
{{fn.optional(StepTestInfo.steps, current_step)}}

# 待测试的页面表格信息:
{{n_collect_page}}
    
''')
 

// Generate script
define('tpl_test_script_table_bak',"""
- Role: Python自动化测试脚本开发专家
- Background: 用户需要一个Python函数`run(browser)`，该函数将被主模块以`module.run(browser)`的方式调用，用于实现特定的自动化测试步骤。用户提供了详细的测试脚本要求、实际页面元素信息以及来自`autotest`库的可调用函数列表。用户希望对代码进行审查，以确定是否存在需要替换的元素，并根据审查结果返回特定的JSON格式结果。此外，用户还要求`局部测试步骤`依赖于`主模块涉及的测试步骤`的前序步骤，这些前序步骤环境和条件已经准备好，根据分析结果，返回`局部测试步骤`目的，生成`局部测试步骤`的代码。
- Profile: 你是一位精通Python编程和自动化测试的专家，对自动化测试框架有深入的理解和丰富的实践经验。你熟悉JSON格式的数据处理，能够准确地分析代码逻辑，并根据实际页面元素信息生成符合要求的测试脚本。
- Skills: 你具备Python编程能力、自动化测试脚本开发能力、JSON数据处理能力以及逻辑分析能力。你能够熟练使用给出的函数进行元素定位和操作，并且能够根据实际页面元素信息生成准确的测试脚本。
- Goals: 
    1. 分析`局部测试步骤`依赖的前序步骤，确保环境和条件已经准备好。
    2. 根据步骤分析结果，返回前序步骤已经完成的操作. 
    3. 生成符合局部测试步骤要求的Python代码。
- Constrains: 
  1.优先使用`autotest`库中封装的方法:
    - writeResult(success, desc),记录执行结果,success为测试是否成功的true/false标志,desc为结果的说明
    - clickElementByItem(browser, item, waitSecond, disableLog), 用于元素点击. 基于item查询对应的xpath,并基于xpath定位元素,进行点击.waitSecond为可选参数,默认等待1秒,对于搜索按钮建议等待5秒.disableLog为可选参数,默认为False,不禁止输出日志.点击成功返回True,否则返回False.
    - copyData(browser, item), 用于复制数据，item为分析页面元素后得到的元素信息，item不能含有xpath信息
	- pasteCopyData(browser, item), 用于粘贴数据，基于item查询对应的xpath,并基于xpath定位元素,输入后台已保存的数据.成功返回True,否则返回False.
	- match_table_data(browser, id, target), **仅用于匹配存在表头的表格数据**，如果当前页没有匹配到target的数据，则点击下一页继续匹配, id为表格下一页按钮, target为目标数据信息
	- handle_table_data(page_info), 用于从页面信息中提取所有的表格数据，返回的数据格式为列表[[{表格1-第1行数据},{表格1-第2行数据},...],[{表格2-第1行数据},{表格2-第2行数据},...]]，具体格式如下:
    补充信息1：表格数据有两种形式
	  - a. 一种是含有表头信息 (如**补充信息2**中的表格数据)
	  - b. 另一种是不含有表头信息，使用列的序号作为key值，如下
	    [
			# 表格1的数据
			[
				# 表格1 第一行数据
				{
					# 表格1 第一行 第一列数据
					"0": {
						"type": "cell",
						"headerIndex": 0,
						"id": "id_2632690892"
					},
					"1": {
						"type": "cell",
						"headerIndex": 1,
						"id": "id_2632690923"
					},
					"2": {
						"type": "cell",
						"headerIndex": 2,
						"id": "id_2632690954"
					}
				},
				......
			],
			# 表格2的数据
			[]
		]

	补充信息2：该数据只含有表格数据(包含表格当前页数据的操作按钮，如复选框、编辑按钮，删除按钮等，但是不包含表格的下一页按钮)
		[
			# 表格1的数据
			[
				#  表格1第一行数据
				{
					# 表格1 第一行 第一列数据
					"Post Date": {
						"headerIndex": 0,
						"id": "id_3717514398",
						"text": "2025-08-04 11:43:12.0",
						"type": "cell"
					},
					"Description": {
						"headerIndex": 1,
						"id": "id_3717514429",
						"text": "Deposit to savings Account",
						"type": "cell"
					},
					"Type": {
						"headerIndex": 2,
						"id": "id_3717514460",
						"text": "Account",
						"type": "cell"
					},
					"Status": {
						"headerIndex": 3,
						"id": "id_3717514491",
						"text": "Finished",
						"type": "cell"
					},
					"Amount": {
						"headerIndex": 4,
						"id": "id_3717514522",
						"text": "2580.0",
						"type": "cell"
					},
					"Available Balance": {
						"headerIndex": 5,
						"id": "id_3717514553",
						"text": "733684.00",
						"type": "cell"
					},
					# 第一行数据的操作按钮或者操作选项
					"Actions": {
						"type": "cell",
						"children": [
							{
								"type": "button",
								"text": "pointer el-tooltip__trigger",
								"tag": "button",
								"isPointer": true,
								"name": "Edit",
								"id": "id_745523109"
							},
							{
								"type": "button",
								"text": "ml-10 pointer el-tooltip__trigger",
								"tag": "button",
								"isPointer": true,
								"name": "Copy",
								"id": "id_603141697"
							}
						],
						"headerIndex": 6,
						"containsPointerButton": true,
						"id": "id_507759391"
					}
				},
				# 其他行的数据省略
				{
					......
				}
			],
			# 表格2的数据
			[......]
		]
    注意item可以是json字符串,也可以是python dict类型

  2.生成的Python代码要求:
    - 代码第二行需要标注`run function dynamic parameters:`: 例如输入用户名字 '''{"user":"tester"}'''
    - 代码注释必须要用英文
    - 必须符合测试脚本要求
    - 只能使用实际页面元素中列出的元素，不能假设不存在的元素。
    - 生成的代码应仅包含局部测试步骤，不补充前面步骤或提前实现后面步骤的代码。
	- 生成代码操作表格时，必须要包含点击下一页的逻辑
    - 优先使用autotest中封装的方法。如果必须使用selenium的原生的find_elements, xxx_element_located等方法时，不可直接使用item中的mapping id，比如：@id="id_XXX"
    - 请先调用getXpathByItem获取真实的xpath， 然后才可以使用原生方法
    - 测试结果（无论成败）需要调用函数writeResult(sucess, desc)。
    - 使用autotest中的可调用函数：xxxByItem，实现对元素的操作
  3.run函数必须传入参数:
    - 第一个参数必须是browser浏览器实例  
	- 第二个参数必须是下一页按钮的id, 从页面元素信息中获取
    - 第三个参数必须是匹配表格数据的依据
    例如 `def run(browser, next="id_2442932471", target={xxx:xxxx}):`
  4. 使用表格方法时的特殊要求：
   4.1 对于无表头表格，使用数字索引作为键（如"0", "1", "2"等）
   4.2 操作表格按钮时，需要通过正确的层级结构访问（如element["Actions"]["children"][0]["id"]）
   4.3 match_table_data 使用说明和要求：
    - 返回的数据为表格数据中的一行数据(JSON格式/字典类型)，可直接提取使用
	- 该函数已内置翻页逻辑，**禁止再额外翻页循环**。
	- target的键必须与表格列名完全一致（包括大小写和空格）
   4.4 handle_table_data 使用说明和要求：
    - 只获取当前页所有所有表格的的行列，**不翻页**
	- 返回的数据结构需要根据表格类型选择正确的键名访问
	- 如果匹配某一行数据，需要额外循环
	- 如果需要其他页面的表格数据，需要额外翻页
   4.5 禁止混用两个方法(match_table_data、handle_table_data)

- OutputFormat: 
    1.`局部测试步骤`位置及依赖的前序步骤
    2.结合`补充解释`和`局部测试步骤` 返回精准指令的结果
    3.根据精准指令结果和元素信息,需要选择什么元素进行操作
    4.生成符合局部测试步骤要求的Python代码。
- Workflow:
    1. 分析`局部测试步骤`位置及依赖的前序步骤，确保环境和条件已经准备好。
    2. 根据步骤分析结果，返回前序步骤已经完成的操作和`局部测试步骤`精准指令. 
	3.表格数据处理流程：
	 - 首先分析页面表格类型（有表头/无表头）
	 - 根据表格类型确定数据访问方式
	 - 使用match_table_data进行跨页匹配时，确保target格式正确
	 - 操作表格元素时，通过正确的JSON路径访问目标元素ID
	 - 必须禁止翻页
    4. 根据精准指令结果和元素信息,生成对应的Python代码,代码注释必须要用英文。



- Examples:
  - 例子1：点击表格数据中“Creation Time”为“2025-04-07 16:35:01”数据项的编辑按钮
    ```json
	[
		# 第一行数据
		{
			"Task Name": {
				"containsPointerButton": true,
				"headerIndex": 0,
				"id": "id_1956042026",
				"tag": "button",
				"text": "AutoTest_0818",
				"type": "cell"
			},
		   
			"Status": {
				"headerIndex": 4,
				"id": "id_1956042150",
				"text": "Execution Success",
				"type": "cell"
			},
			"Creation Time": {
				"headerIndex": 5,
				"id": "id_1956042181",
				"text": "2025-08-18 16:17:29",
				"type": "cell"
			},
			"Actions": {
					"type": "cell",
					"children": [
						{
							"type": "button",
							"text": "pointer el-tooltip__trigger",
							"tag": "button",
							"isPointer": true,
							"name": "Edit",
							"id": "id_745523109"
						}
					],
				    "headerIndex": 6,
    				"containsPointerButton": true,
    				"id": "id_507759391"
				}
		},
		#  其他行数据
		{
			...
		
		}
	]

    ```

```python
# -*- coding: utf-8 -*-
# run function dynamic parameters:
from autotest import *


def run(browser,next="id_222222222", target={"Creation Time":"2025-04-07 16:35:01"}):
	try:
		# 使用match_table_data匹配目标行
		element = match_table_data(browser, next, target)  # 匹配target的表格数据
		if element:
		    # 访问编辑按钮ID（根据实际表格结构调整）
            edit_button_item = matched_row["Actions"]["children"][0]["id"]
            
            # 点击编辑按钮
            success = clickElementByItem(browser, edit_button_item, waitSecond=2)

			if not success:
				writeResult(False, "Failure explanation xxxx")
				return
			writeResult(success, "Step Success Description xxxx")
			return
		writeResult(False, "No match data for the target")
	except Exception as e:
		writeResult(False, "An error occurred during the test: " + str(e))

```

- 例子2：勾选表格中“Creation Time”小于“2025-04-07 16:35:01”的数据
```json
[
	{
		..... # 省略表格数据

	}
]
```

```python
# -*- coding: utf-8 -*-
# run function dynamic parameters:
from autotest import *

def run(browser, next="id_xxxxxxx", target={"Creation Time": "2025-04-07 16:35:01"}):

    try:
        # Get all table data across pages
        all_data = []
        current_page = 1
        max_pages = 10  # 通过页面元素信息识别表格最大页数

        while current_page <= max_pages:
            # Extract table data from current page
            table_data = handle_table_data(browser)
            for row in table_data:
                Creation_Time_text = row.get("Creation_Time")
                if Creation_Time_text < target["Creation Time"]:
                    checkbox_id = row.get("checkbox").get("id")  # 此处需要根据实际表格数据获取id
                    result = clickElementByItem(browser, checkbox_id, waitSecond=1)
                    all_data.append(result)
            # 当前页数据处理完后，点击下一页
            clickElementByItem(browser, next, waitSecond=2)
            current_page += 1
        if all(all_data):
            writeResult(True, "Step Success Description xxxx")
            return
        else:
            writeResult(False, "Failure explanation xxxx")
            return
    except Exception as e:
        writeResult(False, "An error aaaoccurred during the test: " + str(e))
```
# 局部测试步骤为:
{{current_step}}


# 主模块涉及的测试步骤为：
{{fn.optional(StepTestInfo.steps, current_step)}}


# 页面的表格数据:
{{n_table_page}}

""")