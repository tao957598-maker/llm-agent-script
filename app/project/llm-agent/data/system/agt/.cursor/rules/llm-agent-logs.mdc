---
description: llm-agent 日志分析规则，包含日志目录、核心概念、过滤方法和常见问题排查
alwaysApply: true
---

# llm-agent 日志分析规则

## 项目结构

```
F:\gientech-repository\          # llm-agent 项目根目录
├── logs\                         # llm-agent 主日志目录
│   ├── agent-service.log
│   ├── performance.log
│   └── ssemsg.log
├── llm-agent\                    # llm-agent 主服务（独立仓库）
└── llm-agent-core\               # llm-agent 依赖包（独立仓库）
```

> 注意：`D:\app\project\llm-agent\data\...` 是 llm-agent-core 的日志目录

## 用户级日志目录（llm-agent-core）

```
D:\app\project\llm-agent\data\user\{username}\logs\{YYYYMMDD}\{requestId}.log
```

| 目录层级 | 说明 |
|---------|------|
| `username` | 用户目录名，如 `admin` |
| `YYYYMMDD` | 日期目录，如 `20260421` |
| `requestId` | 日志文件名，如 `aaaaaa.log` |

### 示例

```
D:\app\project\llm-agent\data\user\admin\logs\20260421\aaaaaa.log
```

- 用户：admin
- 日期：2026-04-21
- requestId：aaaaaa

> 此目录仅记录 llm-agent-core 工程的日志，优先分析 llm-agent 主日志

## 日志目录

### llm-agent 主日志

```
F:\gientech-repository\logs\
```

| 文件 | 说明 |
|------|------|
| `agent-service.log` | 主日志，记录整个项目的业务日志 |
| `performance.log` | SQL日志，可查看慢查询 |
| `ssemsg.log` | SSE推送日志，实时推送相关 |

- logback 配置输出到 `./logs/agent-service.log`

## 核心概念

| 概念 | 数据库字段 | 说明 |
|------|-----------|------|
| **traceId** | - | 链路跟踪ID，前端每个请求生成唯一ID |
| **requestId** | `llm_agent_instance.requestId` | Agent运行实例的唯一请求ID |
| **bizId** (instanceId) | `llm_agent_instance.ID` | Agent运行实例的ID，一个bizId可有多个requestId |
| **agentId** | `log_llm_agent_main.ID` | Agent调用ID，主agent有多个子agent时会产生多个agentId |

## 关联关系

```
traceId (请求链路)
    └── requestId (请求标识)
          └── bizId/instanceId (实例ID)
                └── agentId (agent调用ID，可能多个)
```

## 日志文件命名与过滤

### 按 traceId 过滤

- traceId 出现在日志行中，格式：`"traceId":"xxx"` 或 `traceId=xxx`
- 用途：追踪前端某个请求的完整链路

### 按 requestId 过滤

- requestId 出现在日志行中，格式：`"requestId":"xxx"` 或 `requestId=xxx`
- 用途：查看某个请求的agent执行记录

### 按 bizId/instanceId 过滤

- bizId 是 instance 表的主键ID
- 用途：查看某个实例的所有运行记录

### 按 agentId 过滤

- agentId 是 log 表的主键ID
- 用途：查看单个agent调用的详细日志

## 日志分析方法

### 1. 追踪完整请求链路

```bash
grep "traceId=abc123" F:/gientech-repository/logs/agent-service.log
```

### 2. 查看某次Agent运行的全部日志

```bash
grep "requestId=req-456" F:/gientech-repository/logs/agent-service.log
```

### 3. 查看单个Agent实例的日志

```bash
grep "bizId=789" F:/gientech-repository/logs/agent-service.log
```

### 4. 查看某次Agent调用的详细日志

```bash
grep "agentId=101" F:/gientech-repository/logs/agent-service.log
```

## 日志级别

| 级别 | 说明 |
|------|------|
| ERROR | 错误，影响流程 |
| WARN | 警告，可能有问题 |
| INFO | 一般信息 |
| DEBUG | 调试信息 |

## 常见问题排查

### 问题：前端请求无响应

1. 用 traceId 在 `agent-service.log` 中过滤
2. 检查是否有 ERROR 或异常
3. 检查 traceId 链路中是否有环节中断

```bash
grep "traceId=abc123" F:/gientech-repository/logs/agent-service.log
```

### 问题：SQL执行慢

1. 查看 `performance.log`
2. 搜索可疑的 SQL 或执行时间

```bash
grep "SELECT" F:/gientech-repository/logs/performance.log
```

### 问题：SSE推送异常

1. 查看 `ssemsg.log`
2. 检查推送状态和错误信息

```bash
grep "error" F:/gientech-repository/logs/ssemsg.log
```

## 快速检查清单

- [ ] 查看日志前先确认是 llm-agent 主日志还是 llm-agent-core 用户日志
- [ ] 根据 traceId/requestId/bizId/agentId 选择合适的过滤方式
- [ ] Windows 下使用 Git Bash 执行 grep 命令
- [ ] 路径使用正斜杠 `/`
