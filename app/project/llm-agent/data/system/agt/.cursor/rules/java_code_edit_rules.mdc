# Java 代码修改规则

## 核心原则

**修改 Java 代码时，禁止改变原有编码格式。**

必须保证修改后的文件能被 IDEA 正常编译和识别。

---

## 编码要求

### 文件编码

- **目标编码**：`UTF-8 without BOM`
- **禁止**：添加 BOM 头（`EF BB BF`）
- **禁止**：转换为 GBK、GB2312 等其他编码

### 如何检查

1. **VSCode/Cursor**：右下角显示 `UTF-8`（不带 `BOM` 或 `LF` 之类）
2. **命令行检查 BOM**：
   ```bash
   # 检查文件开头是否为 EF BB BF
   head -c 3 filename.java | xxd
   ```

### IDEA 配置建议

在 `idea64.exe.vmoptions` 中添加（防止乱码）：
```properties
-Dfile.encoding=UTF-8
```

---

## 修改规范

### 1. 禁止事项

| 禁止操作 | 原因 |
|---|---|
| ❌ 改变文件编码 | 导致 IDEA 编译失败 |
| ❌ 添加 BOM 头 | Java 编译器报错 |
| ❌ 转换行尾符（可选） | 可能影响 Git diff |

### 2. 正确做法

```java
// ✅ 保持原有编码，直接修改代码
// 只修改需要改动的部分，不要格式化整个文件

// ✅ 添加新代码时使用原有风格
// 保持缩进、命名风格一致

// ✅ 修改时保留原有注释
// 不要随意删除或修改原有的业务注释
```

### 3. 格式化规则

- **不要**自动格式化整个文件（可能改变行尾符）
- 只格式化改动的部分
- 如需格式化，事先告知用户并获得确认

---

## 常见问题

### 问题：BOM 导致编译失败

```
错误: 非法字符: '\ufeff'
public class Test {
```

**原因**：文件保存为 `UTF-8 with BOM`

**解决**：用 VSCode 重新 Save As 为 `UTF-8`

### 问题：中文乱码

**原因**：文件编码与 IDEA 编码不一致

**解决**：
1. 确认文件是 `UTF-8` 编码
2. IDEA 设置：`Settings → Editor → File Encodings → UTF-8`

---

## 快速检查清单

- [ ] 修改文件时右下角显示 `UTF-8`？
- [ ] 没有添加 BOM 头？
- [ ] 没有改变原有编码格式？
- [ ] IDEA 能正常编译此文件？
