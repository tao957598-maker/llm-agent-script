/*
{
	"title": "OpsClaw PLAN",
	"description": "OpsClaw PLAN - 根据用户输入与 INIT 上下文生成任务列表，并解析为 plan list。",
	"parameter": [
		{"name":"userInput", "label":"用户输入", "required":true},
		{"name":"loginUserName", "label":"登录用户名"},
		{"name":"contextSystem", "label":"当前系统"},
		{"name":"contextEnv", "label":"当前环境"},
		{"name":"n_load_memory_result", "label":"记忆加载结果"},
		{"name":"n_api_candidates", "label":"API 候选"},
		{"name":"n_dynamic_toolset_guide", "label":"动态工具集说明"},
		{"name":"n_tool_routing_guide", "label":"工具路由规则"}
	],
	"resultPlan": "n_plan_tasks",
	"discoverable": false,
	"tag": "opsclaw"
}
*/

define('tpl_planner_task', '''
你是 opsclaw_assistant 的规划器。

目标：把用户需求拆成一个简洁、可执行、便于后续 ReAct 循环消费的 markdown 任务清单。

要求：
1. 最多输出 5 个待办步骤。
2. 仅输出 markdown task list，不要解释。
3. 优先先做信息收集，再做工具执行，最后输出总结。
4. 如果任务本质是简单问答，也至少输出 2 个步骤：理解问题、组织答案。
5. 若存在高风险动作，应在步骤中显式出现“确认”或“dry-run”。
6. 如果当前无法直接确定该调用哪个 API 或 agent，可安排一步调用 `agt_all_scenario_dynamic_toolset.toolset` 做动态工具发现与执行。

# 用户输入
{{userInput}}

# 用户上下文
- loginUserName: {{loginUserName!}}
- contextSystem: {{contextSystem!}}
- contextEnv: {{contextEnv!}}

# opsclaw 记忆检索结果
{{n_load_memory_result.context!}}

# opsclaw 人格配置（profile.md）
{{n_load_memory_result.profile!}}

# opsclaw 关键记忆（key-memories.md）
{{n_load_memory_result.keyMemories!}}

# 可参考 API 候选
{{n_api_candidates!}}

# 可参考动态工具集 agent
{{n_dynamic_toolset_guide}}

# 工具路由规则
{{n_tool_routing_guide}}
''')

tpl_planner_task, psr_all
//## 解析 LLM 规划结果为结构化任务列表
n_plan_tasks, act_plan.parse?task={{sys_last_result}}
