/*  
{
    "title": "返回知识库结构信息",
	"parameter": "excelFilename=要保存的Excel文件名&columns=从excel中挑选出来的要保存的字段",
	"description": "columns和vector、scalar选填",
	"category": "知识库"
}   
*/


act_func.readTwoRows?filename={{excelFilename}}
tpl_define_excel_columns


	
define('tpl_define_excel_columns', '''
	你是一名高级的Java开发工程师，请基于vector字段、excel数据，以及可参考的知识库，构建tableName为{{tablename}}的milvus知识库，请返回定义知识库的JSON格式(id字段为null)，请默认使用excel列名作为comment字段，请不要增加vector字段。
	# schema和tableName及所有字段定义规则：
	  schema、tableName、name三个属性的值采用全小写英文的下划线命名法（比如AbcDef -> abc_def、abcDef -> abc_def、abc-def -> abc_def、ABC_DEF -> abc_def）

	# 向量库vector字段要求：
	1. vector向量的维度为384
	2. vector字段命名格式为dense_xxx
	3. name采用全小写英文的下划线命名法（比如AbcDef -> abc_def、abcDef -> abc_def、abc-def -> abc_def、ABC_DEF -> abc_def）
	4. comment字段采用对应的excel的表头的值，方便后续的识别程序进行匹配检索
	
	# vector字段(为空表示不用向量字段)：
	  {{vector!}}
	# scalar字段(为空表示不用标量字段)：
      {{scalar!}}
    # 特别要求：
        - **如果 {{vector!}} 和 {{scalar!}} 两者都为空或不存在时：**
            → 所有字段的 vectorType 必须为 null，dimension 必须为 null（表示纯非向量库字段）。

        - 当字段属于 vector 字段时：
            → vectorType = "Vector"
            → dimension = 384
            → nullable = false

        - 当字段属于 scalar 字段时：
            → vectorType = "Scalar"
            → dimension = null

	# excel数据：
	{{sys_last_result}}
	
	# 可参考的知识库：
	{{fn.query('MetaTableInfo', 'tableName=kb_testcase')}}
	
	# 定义知识库的JSON格式参考：
	{
        "columnInfos": [
            {
                "columnDefault": "",
                "columnType": "varchar",
                "comment": "类别路径",
                "dimension": null,
                "vectorType": "Vector",
                "maxLength": null,
                "name": "category_path",
                "nullable": true
            }
        ],
        "comment": "kb_test_knowledge_123知识库",
        "schema": "default",
        "tableName": "kb_test_knowledge_new4",
	}
	其中：
    向量字段：vectorType 必须为 "Vector"，并填写 "dimension": 384，nullable 设为 false
    标量字段：vectorType 必须为 "Scalar"，dimension 设为 null
    非向量库字段：vectorType 必须为 null，dimension 设为 null
''')

define('tpl_excel_title', '''
	请检查json数据中的comment字段，和excel数据中的表头进行逐个比较，是否能找到一个一模一样的值，并按照检查结果的json格式，给出检查结果。
	
	# json数据：
	{{tpl_define_excel_columns}}

	# excel数据：
	{{agt_python}}
	
	# 检查结果的json：
	{
		type: 'verifyResult',
		success: false(或者true),
		result: xxx不在excel的表头中(如果success为true，不用提供result)
		thought: 思考的过程
	}
''')