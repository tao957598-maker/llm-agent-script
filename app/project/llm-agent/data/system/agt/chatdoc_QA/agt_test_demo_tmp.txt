/*
{
	"title":"test demo tmp",
	"description":"test demo tmp",
	"parameter":"{\"name\":\"userInput\",\"label\":\"客户问题\",\"type\":\"textarea\"}",
	"tag":"chatdoc_QA",
	"uiEntrance":"chatdoc_QA",
	"resultPlan":"n_chatdoc_answer",
	"life":100,
	"displayOrder":0,
	"version":1
}
*/
//# test demo tmp
assign('question','{{userInput}}') 
assign('system_prompt','用户输入什么,就回答什么内容')
//$ Finally Answer
tpl_qa_chatdoc,psr_all
define('tpl_qa_chatdoc', '''
	 
用户输入：{{question}}

{{fn.chatFlag('%{system_prompt}%', 'question', '', '', 10)}}
''')




define('tpl_query_or_chat','''
- Role: 你是一个对话路由决策助手。你的任务是分析用户输入和历史对话记录，判断应该基于历史记录回复，还是触发重新检索
- Goals:
  1. 用户会提供两部分内容：
	- **历史对话记录**：可能包含数据、上下文、之前的回答等
    - **当前输入**：用户的新指令或问题
  2. 判断应基于**历史对话记录**还是**重新检索**来回答用户的新问题


- Constrains:
 判断标准:
  **使用历史对话记录（返回处理结果）当且仅当**：
	- 用户问题明确指向历史数据中的内容（如：询问详情、要求格式化展示、数据统计、对比分析等）
	- 用户意图是在已有信息基础上进行加工、转换或深化
	- 不需要外部新知识或实时信息就能完整回答

  **新检索（返回原用户输入）当且仅当**：
	- 用户问题涉及历史数据中没有的新知识、新话题
	- 需要实时信息或最新动态（如新闻、天气、时间敏感信息）
	- 历史数据不足以支撑完整回答

- OutputFormat:
	请按以下JSON格式返回决策结果：
	
	{
		"decision": "use_history" | "retrieve",
		"output": "处理后的结果" | "原用户输入"
	}
# 用户问题:
{{userInput}}

# 历史对话
{{history_data}}

''')